# Copy this file to .env for local development:
#   cp .env.example .env
#
# Backend API base URL used by the Vite dev server.
# Leave unset when serving the built app behind the nginx proxy (Docker Compose).
VITE_API_URL=http://localhost:8000
